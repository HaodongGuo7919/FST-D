# FST-D

This repository provides the model and training code for FST-D, a deep learning framework for displacement field prediction.

## Files

- `FST-D_model.py`: model definition of the proposed FST-D network.
- `FST-D_train.py`: training script for supervised displacement field learning.

## Requirements

The code was developed with Python and PyTorch. The main dependencies include:

```text
torch
torchvision
numpy
scipy
Pillow
einops
timm
